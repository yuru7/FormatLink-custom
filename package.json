{
  "name": "format-link-custom",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test",
    "manual-test": "node manual-test/server.js",
    "preview:modal": "node manual-test/modal-preview.js",
    "package": "node scripts/package.mjs",
    "build": "npm test && npm run package"
  }
}